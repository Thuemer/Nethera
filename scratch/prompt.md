/opsx:explore

We have an application in "Backend" and "Website". Database is started with "Backend/Nethera/docker-compose.yml" We want to use keycloak for authorization and authentication. I need a docker-compose-file that starts keycloak and it's own database. We want to use postgres as our database. Do not make changes, propose only.

No, i want to have both in "Backend/Nethera/docker-compose.yml".

No, i want to have port 5432 of postgres exposed.